{
  "name": "demo-metric-producer",
  "version": "1.0.0",
  "description": "Lambda function that puts custom CloudWatch metrics",
  "main": "index.js",
  "dependencies": {
    "aws-sdk": "^2.1500.0"
  },
  "scripts": {
    "package": "zip -r ../lambda_function.zip ."
  }
}
